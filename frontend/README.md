# AtechSkills Frontend

Next.js marketing website and LMS portals.

## Run

```bash
npm install
npm run dev
```

Open `http://localhost:8000`.

## Environment Variables

Create `frontend/.env.local` locally and add the same variables in the Vercel frontend project.

```env
NEXT_PUBLIC_APP_URL=https://atechskills.com
NEXT_PUBLIC_CANONICAL_URL=https://atechskills.com
NEXT_PUBLIC_API_URL=https://your-backend-domain.vercel.app/api/v1
NEXT_PUBLIC_SITE_NAME=AtechSkills
```

Do not commit real `.env.local` values to GitHub. Add these values in Vercel under Project Settings > Environment Variables.

## Vercel Frontend

Deploy this folder as its own Vercel project with **Root Directory** set to `frontend`.

- Install command: `npm install`
- Build command: `npm run build`
- Output: Next.js, handled by Vercel automatically

## Structure

- `app` - route segments for public pages, auth, dashboards, sitemap, robots
- `components` - reusable UI, layouts, forms, cards
- `lib` - data, helpers, API client, metadata utilities

The frontend is deployment-ready for Vercel. Set `NEXT_PUBLIC_API_URL` to the deployed backend API URL.

## Production Domain: atechskills.com

Use these settings when replacing the old Hostinger site with this Vercel frontend.

1. In Vercel, open the frontend project.
2. Go to **Settings > Domains**.
3. Add `atechskills.com`.
4. Add `www.atechskills.com` and redirect it to `atechskills.com`.
5. In Hostinger DNS, point the apex/root domain to Vercel:
   - Type: `A`
   - Name: `@`
   - Value: `76.76.21.21`
6. In Hostinger DNS, point `www` to Vercel:
   - Type: `CNAME`
   - Name: `www`
   - Value: `cname.vercel-dns.com`
7. In the Vercel frontend environment variables, set:
   - `NEXT_PUBLIC_APP_URL=https://atechskills.com`
   - `NEXT_PUBLIC_CANONICAL_URL=https://atechskills.com`
   - `NEXT_PUBLIC_API_URL=https://apiatechskills.vercel.app/api/v1` or your final backend API domain
8. Redeploy the frontend after updating DNS/environment variables.

SEO files generated by the app:

- `https://atechskills.com/sitemap.xml`
- `https://atechskills.com/robots.txt`
- `https://atechskills.com/manifest.webmanifest`
