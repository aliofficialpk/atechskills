import { WhatsAppAdminDashboard } from "@/components/whatsapp-admin";

export const metadata = {
  title: "WhatsApp Admin | AtechSkills",
  robots: { index: false, follow: false }
};

export default function Page() {
  return <WhatsAppAdminDashboard />;
}
